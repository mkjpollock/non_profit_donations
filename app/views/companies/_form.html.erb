<%= form_for @company do |f| %>
  <div class="form-group margin-high">
    <%= f.text_field :name, placeholder: "Name" %>
  </div>
  <div class="form-group">
    <%= f.text_area :description, cols: 35, rows: 7,placeholder: "Description" %>
  </div>
  <%= f.submit class: "btn btn-success" %>
<% end %>
